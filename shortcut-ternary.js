// Shortcut
const value = null;
const result = value && "Hello";
console.log(result);
// Ternary
let values = 5;
const results = values > 10 ? "greater than 10" : "less than or equal to 10";
console.log(results);
